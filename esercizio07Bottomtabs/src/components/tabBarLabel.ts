import { StyleSheet} from 'react-native';

const tabBarLabel = StyleSheet.create({
  stile: {
    fontSize: 13,
    fontWeight: 'bold',
    color: 'white',
    padding: 5,
  },
});

export default tabBarLabel;
